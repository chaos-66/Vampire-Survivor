# 验收标准

## 先前检查点 Green

DIFFICULTY 关闭提交为 `84ab53b`。辅助指针生命周期为 Green。

## CP-M4-OUTCOME-01

### 自动化验收

- [x] `resolveRunOutcome`：`running` / `won` / `lost`；`lost` 优先于 `won`
- [x] `clampDtToRunRemaining`：59.98+0.05 -> 0.02；不会超过 60
- [x] `updateGame` 在终局时冻结；结果保持不变
- [x] `pending` 冻结时间（选择过程中不会获胜）
- [x] 接触伤害使 HP 降至 0 -> `lost`；`pending`+0 HP -> `lost`
- [x] `createGameState` 完整重置字段
- [x] 重新开始按钮布局命中测试
- [x] `isRestartCode` 的 `KeyR`
- [x] 审计修复后共计 203 项回归测试
- [x] 已记录 `npm test` / `tsc` / `build` / `audit`

### 浏览器验收

- [x] 完整 OUTCOME 交互检查清单：用户报告为 **PASS**；未提供浏览器及版本

### 流程验收

- [x] OUTCOME 检查点提交（`dfbe925`）
- [x] 首次独立审计为 FAIL；发现的问题已在本地修复
- [x] 全新独立复审为 PASS
- [x] OUTCOME 修复检查点（`a596253`）
- [x] 用户浏览器验收为 PASS
- [x] M5+ 尚未开始

## FINAL-MVP-AUDIT-01

- [x] 审计基线 `6236896` 上的 `main` 干净
- [x] `npm test` 通过两次：8 个文件 / 203 项测试
- [x] `npx tsc --noEmit` 通过
- [x] `npm run build` 通过：43 个模块
- [x] `npm audit` 通过：0 个漏洞
- [x] Git 空白字符和 OUTCOME 范围检查通过
- [x] 已审查 M0-M4 检查点的代码、测试、提交和证据
- [x] 所有 MVP 范围锁定能力均已满足
- [x] M1、M2、M3、架构回归、WORLD、DIFFICULTY、辅助指针和 OUTCOME 均有浏览器证据
- [x] 已记录接受的残余风险
- [x] 独立最终结论：**PASS**
- [x] M4 / MVP 可以宣布为 Green
- [x] 最终纯文档收尾（`0d0289e`）
- [x] M5+ 仍未开始

## CP-OPS-PIPELINE-01

- [x] 强制流程已记录在 `docs/PIPELINE.md` 中
- [x] 流程已加入 `AGENTS.md` 必读列表
- [x] 流程已加入 `docs/WORKFLOW.md` 的开始/恢复流程
- [x] M5 基础检查点已按明确依赖关系排序
- [x] 已定义实现、验证、独立审计、浏览器和收尾关卡
- [x] 已定义本地 Git 检查点规则
- [x] 已定义确定性的 GitHub 同步点和禁止强制推送策略
- [x] 已定义上传证据要求
- [x] 流程检查点（`74b6a3e`）
- [x] 已配置用户确认的 GitHub 远程仓库
- [x] 当前 Green 基线已上传至 GitHub，直至 `04143ba`

## CP-OPS-CHINESE-DOCS-01

- [x] `README.md` 面向人的自然语言使用简体中文
- [x] `AGENTS.md` 与全部 `docs/*.md` 面向人的自然语言使用简体中文
- [x] 强制中文规则写入 `AGENTS.md` 和 `docs/PIPELINE.md`
- [x] 命令、路径、代码标识符、Git 提交原文和专有技术名允许保留原文
- [x] 中文文档实现提交 `67eb0dc` 已同步 GitHub
- [x] 首次正式独立审计为 FAIL；发现的问题已进入审计修复
- [x] 审计修复 `15b474c` 已提交并同步 GitHub
- [x] 全新独立复审为 PASS
- [x] 中文文档检查点已达到 Green 关闭条件

## CP-M5-RUNTIME-01

### 自动化验收

- [x] 批量击杀一次追加对应数量的经验宝石，ID 连续且不设置数量上限
- [x] 批量掉落只构建一次合并结果，不为每个击杀复制已有宝石数组
- [x] 投射物碰撞保持按投射物 ID、敌人 ID 的确定性顺序和一弹一伤语义
- [x] 投射物碰撞不再为每颗投射物重复筛选和排序全部敌人
- [x] 纯函数可正确判断圆形实体与视口相交，包括边缘接触和绘制边距
- [x] 大量屏幕外宝石、敌人和投射物不会进入 Canvas 实体绘制调用
- [x] 绘制裁剪不删除实体，也不改变模拟、碰撞、拾取或目标选择
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过
- [x] `npm run dev` 已启动，`http://localhost:5173/` 返回 HTTP 200

### 浏览器验收

- [x] 游戏可通过实际开发服务器加载，移动、战斗、经验掉落和拾取正常
- [x] 实体穿过视口边缘时无明显过早消失，敌人血条显示正常
- [x] 浏览器验收在实际观察前保持 `UNVERIFIED`

### 流程验收

- [x] 范围仅限运行时性能、批量经验掉落和可见绘制裁剪
- [x] 不引入后续敌人、掉落、世界物体、效果、内容或 NPC 架构
- [x] 实现检查点 `07be558` 已提交并同步 GitHub
- [x] 首次正式独立审计为 FAIL；发现视口角点圆形相交假阳性
- [x] 审计修复 `330fa59` 已提交并同步 GitHub
- [x] 全新独立复审为 PASS
- [x] 浏览器验收为 PASS；用户报告全部检查通过
- [x] `CP-M5-RUNTIME-01` 已达到 Green 关闭条件
- [x] Green 关闭 `f250741` 已提交并同步 GitHub

## CP-M5-ENEMY-ARCH-01

### 自动化验收

- [x] `EnemyDefinition` 分离静态内容与运行时生命状态
- [x] 敌人注册表同一对象重复注册幂等，同 ID 不同对象抛错并保留原定义
- [x] 内容 bootstrap 重复调用幂等，完整重置后可恢复默认敌人
- [x] 敌人工厂按定义 ID 创建实例，写入必填 `definitionId` 和连续运行时 ID
- [x] 未注册定义 ID 明确抛错，不静默回退到基础敌人
- [x] 两个真实生成入口均通过工厂创建基础敌人
- [x] 基础敌人半径、速度、生命、生成位置和现有玩法保持不变
- [x] 测试专用额外定义可通过工厂创建，无需修改主循环
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过
- [x] 开发服务器 `http://localhost:5173/` 返回 HTTP 200

### 浏览器验收

- [x] 基础敌人仍正常生成、追逐、受击、死亡并掉落经验
- [x] 60 秒单局、升级、胜负和重新开始无回归
- [x] 浏览器验收在实际观察前保持 `UNVERIFIED`

### 流程验收

- [x] 范围仅限敌人定义、注册表、工厂和 `definitionId`
- [x] 不新增第二种默认敌人，不实现通用掉落或后续 M5 内容
- [x] 实现检查点 `fb956cb` 已提交并同步 GitHub
- [x] 独立审计为 PASS
- [x] 浏览器验收为 PASS；用户报告全部检查通过
- [x] `CP-M5-ENEMY-ARCH-01` 已达到 Green 关闭条件
- [x] Green 关闭 `ecf5ffb` 已提交并同步 GitHub

## CP-M5-DROP-ARCH-01

### 自动化验收

- [x] `DropDefinition` 仅保存静态掉落与拾取数据，不保存位置、运行时 ID 或收集状态
- [x] 掉落注册表同一对象重复注册幂等；同 ID 不同对象抛错并保留原定义
- [x] 内容 bootstrap 重复调用幂等；完整重置后可恢复默认经验掉落定义
- [x] 掉落工厂按定义 ID 创建实例并写入必填 `definitionId` 和连续运行时 ID
- [x] 未注册定义 ID 明确抛错，不静默回退到经验掉落
- [x] 每次实际击杀只产生一个默认经验掉落；非致命命中和重复命中不产生掉落
- [x] 未收集掉落实体无数量上限，批量生成保持连续 ID
- [x] 拾取默认经验掉落只增加既有经验，升级选择、胜负和重新开始行为保持不变
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过
- [x] 开发服务器返回 HTTP 200

### 浏览器验收

- [x] 基础敌人死亡后仍产生一个可见、可收集的经验掉落
- [x] 收集经验、升级选择、60 秒胜负和重新开始无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限通用掉落/拾取定义和当前经验适配路径
- [x] 不实现食物、宝箱、稀有度、掉落权重、库存、世界物体、效果、NPC 或其他真实内容
- [x] 实现检查点 `bdb5513` 已提交并同步 GitHub
- [x] 首次和第二次独立审计为 FAIL；文档一致性修复后第三次全新复审为 PASS
- [x] 浏览器验收为 PASS；用户报告全部检查通过
- [x] `CP-M5-DROP-ARCH-01` 已达到 Green 关闭条件
- [x] Green 关闭 `37e0a9a` 已提交并同步 GitHub

## CP-M5-WORLD-OBJECTS-01

### 自动化验收

- [x] 新单局按世界尺寸创建固定、连续 ID 且位于世界内的静态矩形物体
- [x] 默认布局不与初始玩家重叠，重新开始重建相同布局
- [x] 玩家从四边和角落均不能穿过障碍，并可沿障碍边缘滑动
- [x] 大步长移动不能穿越薄障碍；无效输入不产生非有限位置
- [x] 非障碍世界物体不阻挡玩家
- [x] 纯函数可见查询包含内部、边缘接触和绘制边距，排除屏幕外物体
- [x] 可见查询和绘制不删除、重排或修改世界物体
- [x] Canvas 只绘制当前视口相交的世界物体
- [x] 敌人、投射物、掉落、升级、胜负和重新开始既有测试保持通过
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过
- [x] 开发服务器返回 HTTP 200

### 浏览器验收

- [x] 障碍进入/离开视口时正常出现/消失，无明显过早裁剪
- [x] 玩家无法从四边或角落穿过障碍，沿边移动正常且不会卡死
- [x] 移动、战斗、经验、升级、60 秒胜负和重新开始无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限静态世界物体、玩家障碍碰撞、可见查询和绘制
- [x] 不实现注册表、随机地图、空间索引、其他实体障碍碰撞、可破坏物、交互、效果、真实内容或 NPC
- [x] 实现检查点 `e3883a8` 已提交并同步 GitHub
- [x] 独立审计为 PASS
- [x] 浏览器验收为 PASS；用户报告全部检查通过
- [x] `CP-M5-WORLD-OBJECTS-01` 已达到 Green 关闭条件
- [x] Green 关闭 `e3fd3ec` 已提交并同步 GitHub

## CP-M5-EFFECTS-01

### 自动化验收

- [x] 效果注册表同一对象重复注册幂等，同 ID 不同对象抛错并保留原定义
- [x] 未注册效果 ID 明确抛错，不静默忽略或回退
- [x] 即时效果每次应用只执行一次且不进入活动效果列表
- [x] 限时增益和减益只派生有效属性，不改写玩家永久基础值
- [x] `refresh` 重置持续时间但保持单层
- [x] `stack` 叠加至明确上限并刷新持续时间
- [x] 效果按有效战斗时间到期；pending 和终局状态冻结计时
- [x] 移动与武器使用效果派生的有效属性；效果结束后恢复永久基础值
- [x] 重新开始清空活动效果；bootstrap/reset 生命周期保持幂等
- [x] 既有移动、战斗、掉落、升级、世界物体、胜负和重新开始测试保持通过
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过

### 浏览器验收

- [x] 移动、战斗、经验、升级、障碍、60 秒胜负和重新开始无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限即时效果、限时效果、叠加规则和现有系统接线
- [x] 不新增默认效果、UI、特效、伤害类型、抗性、光环、区域效果、真实内容或 NPC
- [x] 实现检查点 `9ddf4f6` 已提交并同步 GitHub
- [x] 前两次独立审计为代码 FAIL；后续代码复审确认修复但流程文档仍有 FAIL；第六次全新复审为 PASS
- [x] 首次修复 `9986b4d` 和第二轮修复 `980232c` 已提交并同步 GitHub
- [x] 浏览器验收为 PASS；用户报告全部检查通过
- [x] `CP-M5-EFFECTS-01` 已达到 Green 关闭条件

## CP-M5-CONTENT-01

### 自动化验收

- [ ] 敌人定义可选 `spawnWeight`；权重选择函数按注册表顺序确定性选择，权重为 0 的敌人不参与生成
- [ ] 默认敌人数值与生成行为保持不变（权重 2，回归既有生成测试）
- [ ] `fast_enemy` 通过注册表参与生成，实例携带正确 `definitionId`
- [ ] `kills` 携带敌人 `definitionId`；未注册敌人定义明确失败，不静默回退
- [ ] 每次实际击杀仍产生一个经验掉落，ID 连续、未收集掉落不设上限
- [ ] 带掉落表的敌人按 `chance` 独立掷骰产出食物/宝箱；chance 越界定义明确失败
- [ ] 食物拾取增加生命且不超过最大生命；宝箱拾取增加经验
- [ ] `PickupResult` 纯函数汇总；`game-loop` 应用生命变化
- [x] 散射武器经升级 offer 获得，**替换**当前持有武器而非叠加；等级 1/2/3 对应 3/4/5 颗弹；达 `maxLevel` 后不再进入候选
- [x] 升级选项按 `offerWeight` 权重随机抽取且位置随机；权重 0 的项永不出现；散射弹权重低于属性升级
- [x] 单局坚持时间 5 分钟（`RUN_DURATION_SECONDS = 300`）；HUD 时间显示为 `mm:ss`；胜负与钳制边界按常量派生
- [x] 升级卡片描述超过卡片宽度时自动换行（最多 2 行，超出省略号），无单行溢出
- [ ] 掉落颜色由 `DropDefinition.color` 数据驱动；绘制层无按 ID 硬编码颜色
- [ ] bootstrap 重复调用幂等；完整重置后可恢复全部默认内容
- [ ] 既有移动、战斗、掉落、升级、世界物体、效果、胜负和重新开始测试保持通过
- [ ] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过

### 浏览器验收

- [x] 两种敌人混合出现（快速敌人更快更脆），血条正常
- [x] 升级时可选散射武器并正常发射三颗弹
- [x] 击杀偶见食物（回血）与宝箱（经验），颜色可区分
- [x] 移动、战斗、经验、升级、障碍、5 分钟胜负和重新开始无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限第一批真实内容与三个通用机制扩展
- [x] 不实现敌人行为钩子、稀有度、效果内容、程序化地图、NPC 或 HUD 新 UI
- [x] 实现检查点 `aa0a999` 已提交并同步 GitHub
- [x] 独立审计为 PASS（含武器语义与升级随机化修复复审）
- [x] 浏览器验收为 PASS；用户回复 `pass`，未提供浏览器名称和版本
- [x] `CP-M5-CONTENT-01` 已达到 Green 关闭条件

## CP-M5-ABILITY-01

### 自动化验收

- [x] 能力注册表沿用对象身份规则：同一对象幂等，同 ID 不同对象抛错并保留原定义
- [x] 未知能力 ID 明确失败，不静默回退
- [x] 严格创建入口要求能力已注册且 `create()` 返回匹配的 `definitionId`（能力为纯状态实例，无实体位置，不设运行时实体 ID）
- [x] 能力不进入 `player.weapons`；散射弹单件替换语义不变
- [x] 斧头作为额外攻击行为周期性触发，不占用武器冷却，命中敌人造成伤害
- [x] 吸经验扩大经验拾取范围或提供吸附，不改变经验值语义
- [x] 追踪弹周期性生成并追踪最近敌人，命中造成伤害
- [x] 升级 offer 可获取能力；重新开始清空能力
- [x] bootstrap 重复调用幂等；完整重置后可恢复默认能力
- [x] 既有移动、战斗、掉落、升级、效果、世界物体、胜负、重新开始与 CONTENT-01 内容测试保持通过
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过

### 浏览器验收

- [x] 升级可选斧头/吸经验/追踪弹并生效（斧头额外攻击、经验范围变大、追踪弹自动命中）
- [x] 散射弹替换语义、5 分钟胜负、选项卡随机、卡片文字无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限能力系统与第一批三个能力
- [x] 不实现 NPC、程序化地图、敌人行为钩子、稀有度、局外成长、HUD 新 UI（除非验收必须）
- [x] 实现检查点 `a5b959d` 已提交并同步 GitHub
- [x] 独立审计为 PASS（第三次全新复审）
- [x] 浏览器验收为 PASS；用户回复 `全部pass`，未提供浏览器名称和版本
- [x] `CP-M5-ABILITY-01` 已达到 Green 关闭条件

## CP-M5-RUN-FLOW-DIFFICULTY-01

### 自动化验收

- [x] 初始状态不推进游戏，等待开始
- [x] 点击"开始游戏"后创建新局并进入游戏
- [x] 暂停时 `updateGame` 不推进玩家、敌人、投射物、掉落、能力、效果、经验、时间与胜负
- [x] 暂停后"继续"恢复同一局
- [x] 暂停后"提前结束"回到主界面
- [x] 从主界面再次开始创建完整新局
- [x] 5 分钟胜负规则不变
- [x] 各难度档 `enemyCap` 明显高于当前且 `spawnInterval` 更短；敌人强度属性不变（D-043：0s 70/0.45、15s 110/0.30、30s 160/0.22、45s 220/0.16）
- [x] 移动、战斗、经验、升级、掉落、障碍、效果、能力均回归通过
- [x] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过

### 浏览器验收

- [x] 打开页面先见主界面和"开始游戏"按钮
- [x] 点击开始后进入游戏；游戏内有暂停按钮
- [x] 暂停后画面/时间/敌人/投射物冻结；继续后同一局恢复
- [x] 提前结束后回到主界面；再次开始是完整新局
- [x] 敌人数量明显更多、压力更高，但单个敌人强度无明显变化
- [x] 5 分钟胜负、HUD `mm:ss`、升级卡片、散射弹、斧头、吸经验、追踪弹、食物/宝箱无回归
- [x] 控制台没有未处理错误

### 流程验收

- [x] 范围仅限运行入口/暂停流程与敌人数量压力（D-042/D-043，NPC 前插入）
- [x] 不实现 NPC、设置菜单、存档、角色选择、难度选择 UI、新敌人行为、新内容；不改敌人强度与 5 分钟胜负规则
- [x] 实现检查点 `9702f19` 与高压档位 `fc93677` 已提交并同步 GitHub
- [x] 独立审计为 PASS（高压档位第四次全新复审）
- [x] 浏览器验收为 PASS；用户回复 `全部pass`，未提供浏览器名称和版本
- [x] `CP-M5-RUN-FLOW-DIFFICULTY-01` 已达到 Green 关闭条件

## CP-M5-TARGETING-01

### 自动化验收

- [ ] 新局默认角色朝右（facing 为 (1, 0)）
- [ ] 非零移动方向更新朝向；静止保留最近一次朝向
- [ ] 散射弹自动瞄准最近敌人（朝向不驱动发射方向）
- [ ] 斧头自动瞄准最近敌人（朝向不驱动投掷方向）
- [ ] 默认投射物仍自动瞄准最近敌人
- [ ] 追踪弹仍飞行中自动转向追踪最近敌人
- [ ] 敌人局部分离：完全重合的敌人被确定性拆开；部分重叠被推开；正常距离不被错误推开；不改变敌人属性
- [ ] 暂停冻结朝向与敌人位置；重新开始重置默认朝向与完整新局
- [ ] 既有移动、战斗、掉落、升级、胜负、暂停、重开、难度、武器、能力测试保持通过
- [ ] `npm test`、`npx tsc --noEmit`、`npm run build`、`npm audit` 和 `git diff --check` 通过

### 浏览器验收

- [ ] 角色朝向指示为圆润克制的方向标记（非尖锐三角），随移动更新、静止保持、新局朝右
- [ ] 散射弹/斧头自动瞄准最近敌人并有效攻击附近敌人；默认弹与追踪弹保持自动瞄准
- [ ] 高压敌群仅部分重叠，不再完全叠为一个位置，仍保留群聚压迫感
- [ ] 暂停冻结、继续、返回主界面、再次开始完整新局无回归
- [ ] 控制台没有未处理错误

### 流程验收

- [ ] 范围仅限朝向状态/视觉指示、自动瞄准恢复与敌人分离（D-044/D-045，NPC 前插入）
- [ ] 不实现鼠标/手柄瞄准、锁定 UI、新武器/能力/敌人/掉落/效果、NPC、角色选择、难度选择、存档
- [ ] 实现检查点已提交并同步 GitHub
- [ ] 独立审计为 PASS
- [ ] 浏览器验收为 PASS
- [ ] `CP-M5-TARGETING-01` 已达到 Green 关闭条件
